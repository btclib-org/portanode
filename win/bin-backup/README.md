# Windows Binary Backups

This folder stores backups created by the update scripts.

- `bitcoin/`: Backups of Bitcoin Core executables (e.g., `bitcoin-qt.exe`).
- `electrum/`: Backups of `electrum.exe`.

Rollback scripts move items from these folders back into `win/bin/`.
