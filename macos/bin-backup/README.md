# macOS Binary Backups

This folder stores backups created by the update scripts.

- `bitcoin/`: Backups of `Bitcoin-Qt.app`.
- `electrum/`: Backups of `Electrum.app`.

Rollback scripts move items from these folders back into `macos/bin/`.
